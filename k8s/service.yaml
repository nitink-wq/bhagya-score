apiVersion: v1
kind: Service
metadata:
  name: bhagya-score
  labels:
    app: bhagya-score
spec:
  type: ClusterIP
  selector:
    app: bhagya-score
  ports:
    - name: http
      port: 80
      targetPort: http
